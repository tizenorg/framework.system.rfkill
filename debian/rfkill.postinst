#!/bin/sh
if [ ${USER} == "root" ]
then
     # Change file owner
     # 1. configurations
     chown 5000:5000 /usr/sbin/rfkill
fi

# Change file permissions
# 1. configurations
chmod 700 /usr/sbin/rfkill
